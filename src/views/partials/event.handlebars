<event
{{#if canClickEvents}}
  onClick="location.href='/event/{{id}}';"
  class="pointer"
{{/if}}
>
  {{#if event.series}}
    <left>
      <series>{{event.series.title}}</series>
      <img src="/banner/{{event.series.id}}">
    </left>
  {{/if}}
  <data>
    <detail>
    {{> (eventPartial event) this}}
    </detail>
    <time>{{dateTime timestamp}}</time>
  </data>
</event>