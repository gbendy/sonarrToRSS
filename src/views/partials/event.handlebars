<event
{{#if canClickEvents}}
  onClick="location.href='{{eventUrl id count ascending useApplicationUrl}}';"
  class="pointer"
{{/if}}
>
  {{#if (showBanner event)}}
    <left>
      <series>{{event.series.title}}</series>
      <img src="{{bannerUrl event.series.id useApplicationUrl}}" alt="{{event.series.title}}">
    </left>
  {{/if}}
  <data>
    <detail>
    {{> (eventPartial event) this}}
    </detail>
    <time>{{dateTime timestamp}}</time>
  </data>
</event>
