<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>{{title}}</title>
    <link rel="icon" type="image/x-icon" href="{{localUrl 'favicon.ico'}}">
    <style>
    {{> css }}
    </style>
    {{#if script}}
    <script>
    {{> (lookup . 'script') }}
    </script>
    {{/if}}
</head>
<body>

    {{{body}}}

</body>
</html>
