<div class="container">
  <div class="centreColumn">
  <heading >Event history for {{instanceName}} (<a href="{{sonarrBaseUrl}}">{{sonarrBaseUrl}}</a>)</heading>
    <events>
      <header>
        <count>Events {{start}}-{{end}} of {{total}}</count>
        <pagesize>Show <select onChange="location.href=`{{browseUrl start undefined undefined useApplicationUrl}}/${this.value}{{ascendingQuery ascending}}`;">
          {{#unless standardCount}}
            <option selected value="{{count}}">{{count}}</option>
          {{/unless}}
          {{#each countPreset}}
          <option {{ifEqual ../count this 'selected' ''}} value="{{this}}">{{this}}</option>
          {{/each}}
        </select> per page </pagesize>
        <sort> <select onChange="location.href=`{{browseUrl start count undefined useApplicationUrl}}?sort=${this.value}`;">
          <option value="descending" {{ifEqual ascending false 'selected' ''}}>Newest First</option>
          <option value="ascending" {{ifEqual ascending true 'selected' ''}}>Oldest First</option>
        </select></sort> 
      </header>
      {{> navigation}}
      {{#each events}}
        {{> event this canClickEvents=../canClickEvents useApplicationUrl=../useApplicationUrl}}
      {{/each}}
      {{> navigation}}
    </events>
  </div>
</div>