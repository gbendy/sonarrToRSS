<div class="container">
  <div class="centreColumn">
    <heading >Event history for {{instanceName}} (<a href="{{sonarrBaseUrl}}">{{sonarrBaseUrl}}</a>)</heading>
    <events class="single">
    <header>
      <count>Event {{event.ascendingIndexDisplay}}</count>
    </header>
    {{> event event useApplicationUrl=useApplicationUrl}}
    <footer><browse><a href="{{browseUrl event.ascendingIndex undefined undefined useApplicationUrl}}">Browse</a></browse></footer>
    </events>
  </div>
</div>